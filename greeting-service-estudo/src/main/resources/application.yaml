server:
  port: 8080
spring:
  application:
    name: greeting-service-estudo
  config:
    import: optional:configserver:http://localhost:8888/
  cloud:
    config:
      profile: es
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'
  endpoint:
    health:
      show-details: ALWAYS

#greeting-service:
#  greeting: 'Olá'
#  defaultValue: 'Mundo'
