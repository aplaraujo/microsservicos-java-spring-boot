server:
  port: 8765
spring:
  application:
    name: api-gateway-estudo
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true